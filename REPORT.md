# 📊 결과보고서: System_Part2 - 비동기 대규모 접속 채팅 서버

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **프로젝트명** | System_Part2 (비동기 대규모 접속 채팅 서버) |
| **개발 언어** | Rust (Edition 2024) |
| **핵심 기술** | Tokio 비동기 런타임, Broadcast Channel, Slint GUI |
| **목표** | 다수의 클라이언트가 동시 접속하여 실시간 메시지를 교환하는 채팅 서버 구현 |

---

## 2. 팀원 및 역할 분담

| 이름 | 담당 역할 |
|------|-----------|
| 함태원 | main.rs 작성, server.rs 작성 |
| 허재성 | 간단한 프론트엔드 작성, client.rs 작성 |
| 황찬우 | tester.rs 작성, Git 관리 |

---

## 3. 개발 목표 및 달성 여부

| # | 목표 | 달성 여부 | 비고 |
|---|------|-----------|------|
| 1 | 비동기 TCP 채팅 서버 구현 | ✅ 달성 | tokio + broadcast channel 기반 |
| 2 | 다중 클라이언트 동시 접속 지원 | ✅ 달성 | tokio::spawn으로 경량 태스크 분리 |
| 3 | 메시지 브로드캐스팅 | ✅ 달성 | 1:N broadcast 패턴 구현 |
| 4 | GUI 클라이언트 구현 | ✅ 달성 | Slint 프레임워크 활용 |
| 5 | 대규모 접속 봇 테스터 구현 | ✅ 달성 | 실시간 수신/출력 방식 |
| 6 | 서버 로그 모니터링 | ✅ 달성 | server.rs에 메시지 로그 출력 |
| 7 | Lagged 에러 핸들링 | ✅ 달성 | graceful한 에러 복구 |

---

## 4. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│              채팅 서버 (main.rs / server.rs)           │
│                                                       │
│  TcpListener (127.0.0.1:8080)                        │
│       │                                               │
│       ▼                                               │
│  ┌─────────────────────────────────────┐             │
│  │     Broadcast Channel (1024 버퍼)     │             │
│  │                                       │             │
│  │   tx ──────┬──────┬──────┬─────      │             │
│  │            │      │      │            │             │
│  │           rx1    rx2    rx3  ...      │             │
│  └─────────────────────────────────────┘             │
│       │              │             │                  │
│  ┌────▼────┐   ┌────▼────┐  ┌────▼────┐            │
│  │ Task #1  │   │ Task #2  │  │ Task #3  │  ...      │
│  │(Client A)│   │(Client B)│  │(Client C)│           │
│  └──────────┘   └──────────┘  └──────────┘           │
└─────────────────────────────────────────────────────┘
         ▲                ▲               ▲
         │                │               │
    ┌────┴────┐     ┌────┴────┐    ┌────┴────┐
    │  GUI     │     │  Bot 0   │    │  Bot 1   │
    │ (client) │     │ (tester) │    │ (tester) │
    └──────────┘     └──────────┘    └──────────┘
```

---

## 5. 핵심 구현 분석

### 5.1 서버 (main.rs / server.rs)

**비동기 이벤트 처리 모델:**

서버는 `tokio::select!` 매크로를 활용하여 두 가지 이벤트를 동시에 대기합니다:

- **이벤트 A**: 클라이언트로부터 메시지 수신 → broadcast 채널로 전파
- **이벤트 B**: broadcast 채널에서 다른 클라이언트의 메시지 수신 → 현재 클라이언트에게 전달

`server.rs`는 추가로 서버 터미널에 `📩 [LOG]` 형태로 메시지를 출력하여 모니터링이 가능합니다.

| 비교 항목 | OS 스레드 모델 | Tokio 비동기 모델 |
|-----------|---------------|------------------|
| 메모리 사용 | 스레드당 ~2MB 스택 | 태스크당 ~수 KB |
| 500명 접속 시 | ~1GB 메모리 필요 | ~수 MB 수준 |
| 컨텍스트 스위칭 | OS 커널 개입 (무거움) | 유저스페이스 스케줄링 (가벼움) |
| 확장성 | 수천 개 한계 | 수만~수십만 가능 |

### 5.2 클라이언트 (client.rs)

**GUI와 네트워크의 분리 설계:**

```
┌──────────────┐     mpsc channel     ┌──────────────┐
│   GUI 스레드   │ ──────────────────▶ │  전송 태스크    │ ──▶ 서버
│  (Slint 이벤트) │                     │  (Task B)     │
└──────────────┘                      └──────────────┘

┌──────────────┐  invoke_from_event   ┌──────────────┐
│   GUI 스레드   │ ◀────────────────── │  수신 태스크    │ ◀── 서버
│  (화면 업데이트) │                     │  (Task A)     │
└──────────────┘                      └──────────────┘
```

- `into_split()`: TcpStream을 읽기 전용(`OwnedReadHalf`)과 쓰기 전용(`OwnedWriteHalf`)으로 분리
- `mpsc` 채널: GUI → 네트워크 방향의 단방향 메시지 전달
- `invoke_from_event_loop`: 네트워크 스레드에서 GUI 스레드로 안전하게 업데이트 요청
- 빈 메시지 전송 방지: UI 콜백에서 `if (self.text != "")` 조건 검사

### 5.3 테스터 (tester.rs)

**실시간 수신/출력 모델:**

```
1. CLIENT_COUNT개 봇 순차 생성 (10개마다 50ms 간격)
2. 각 봇이 접속 즉시 인삿말 전송: "bot {id} hi"
3. 무한 루프로 서버 메시지 실시간 수신 및 출력
4. 서버 연결 종료 시 해당 봇 자동 종료
```

---

## 6. 개발 중 발생한 문제 및 해결 과정

### 6.1 문제: Broadcast 채널의 Lagged 에러로 인한 패닉

**증상**: 다수의 봇이 동시에 메시지를 전송하면 broadcast 채널의 버퍼(1024)가 가득 차서 `RecvError::Lagged` 에러가 발생하고, 이를 처리하지 않으면 `unwrap()`에서 프로그램이 패닉으로 종료됨.

**원인**: `broadcast::channel`은 고정 크기 링 버퍼를 사용하며, 느린 수신자가 아직 읽지 않은 메시지가 새 메시지에 의해 덮어씌워지면 `Lagged` 에러를 반환함.

**해결**:
```rust
Err(tokio::sync::broadcast::error::RecvError::Lagged(_)) => {
    continue; // 놓친 메시지는 건너뛰고 다음 메시지부터 수신
}
```
`Lagged` 에러를 별도로 매칭하여 `continue`로 처리. 일부 메시지를 놓칠 수 있지만 서버가 크래시되지 않고 안정적으로 동작을 유지함.

---

### 6.2 문제: OS 파일 디스크립터 제한으로 대규모 접속 실패

**증상**: 500개 봇을 한꺼번에 `tokio::spawn`하면 OS의 파일 핸들(소켓) 제한에 걸려 `TcpStream::connect`가 실패함. 특히 Windows에서 기본 제한이 낮아 문제가 빈번하게 발생.

**원인**: 각 TCP 연결은 OS 파일 디스크립터를 하나씩 소비하며, 수백 개가 동시에 열리면 시스템 제한에 도달.

**해결**:
```rust
// 10개 봇마다 50ms 간격을 두어 OS가 소켓을 안정적으로 할당할 시간을 줌
if id % 10 == 0 {
    sleep(Duration::from_millis(50)).await;
}
```
봇 생성을 점진적으로(staggered) 수행하여 OS에 부담을 분산. 추가로 `TcpStream::connect` 실패 시 `match`로 에러를 처리하여 한 봇의 실패가 전체 테스트를 중단시키지 않도록 함.

---

### 6.3 문제: GUI 스레드에서 네트워크 I/O 호출 시 UI 프리징

**증상**: 클라이언트에서 메시지 전송 버튼을 누르면 GUI가 잠시 멈추는 현상 발생. Slint의 이벤트 루프가 블로킹됨.

**원인**: GUI 콜백 내에서 직접 `writer.write_all().await`를 호출하면, 비동기 작업이 GUI 스레드를 점유하여 화면 업데이트가 지연됨.

**해결**:
```rust
// mpsc 채널을 통해 GUI → 네트워크 태스크로 메시지를 비동기 전달
let (tx, mut rx) = tokio::sync::mpsc::channel::<String>(32);

// GUI 콜백에서는 채널에 던지기만 함 (논블로킹)
ui.on_send_message(move |msg| {
    let tx = tx_for_ui.clone();
    let formatted_msg = format!("{}\n", msg);
    tokio::spawn(async move {
        let _ = tx.send(formatted_msg).await;
    });
});

// 별도 태스크에서 채널을 읽어 실제 네트워크 전송 수행
tokio::spawn(async move {
    while let Some(msg) = rx.recv().await {
        if writer.write_all(msg.as_bytes()).await.is_err() { break; }
    }
});
```
GUI 콜백 → `mpsc` 채널 → 전송 전담 태스크 구조로 분리하여, GUI 스레드는 절대 블로킹되지 않음.

---

### 6.4 문제: 네트워크 태스크에서 GUI 업데이트 시 스레드 안전성 위반

**증상**: 서버에서 메시지를 수신하는 백그라운드 태스크에서 직접 `ui.set_chat_log()`를 호출하면 컴파일 에러 또는 런타임 패닉 발생.

**원인**: Slint의 UI 컴포넌트는 메인(GUI) 스레드에서만 접근 가능. 다른 스레드에서 직접 조작하면 스레드 안전성이 보장되지 않음.

**해결**:
```rust
let _ = slint::invoke_from_event_loop(move || {
    if let Some(ui) = ui.upgrade() {
        let current_log = ui.get_chat_log();
        ui.set_chat_log(current_log + &new_msg);
    }
});
```
`slint::invoke_from_event_loop`을 사용하여 GUI 업데이트 클로저를 메인 스레드의 이벤트 큐에 안전하게 전달. `Weak` 핸들(`as_weak()`)을 사용하여 UI가 이미 닫힌 경우에도 안전하게 처리.

---

### 6.5 문제: 소켓 소유권 충돌 (읽기와 쓰기 동시 사용)

**증상**: 하나의 `TcpStream`을 읽기 태스크와 쓰기 태스크에서 동시에 사용하려 하면 Rust의 소유권 시스템이 컴파일을 거부함.

**원인**: Rust는 하나의 값에 대해 동시에 여러 가변 참조(`&mut`)를 허용하지 않음. `TcpStream`을 두 태스크에서 동시에 사용하는 것은 소유권 규칙 위반.

**해결**:
- **서버**: `socket.split()`으로 빌려쓰기(borrow) 분리 → 같은 `tokio::spawn` 내에서 `select!`로 동시 처리
- **클라이언트**: `stream.into_split()`으로 소유권 완전 분리 → 별도 태스크에서 독립적으로 사용

```rust
// 서버: 같은 태스크 내에서 split (빌려쓰기)
let (reader, mut writer) = socket.split();

// 클라이언트: 별도 태스크로 분리 (소유권 이전)
let (reader, mut writer) = stream.into_split();
```

---

### 6.6 문제: 빈 메시지 전송으로 인한 불필요한 브로드캐스트

**증상**: 사용자가 입력 없이 전송 버튼을 누르거나 Enter를 치면 빈 줄(`\n`)이 서버로 전송되어 모든 클라이언트에게 빈 메시지가 브로드캐스트됨.

**원인**: UI 콜백에서 입력값 유효성 검사 없이 바로 전송 처리.

**해결**:
```slint
Button {
    clicked => { 
        if (input.text != "") {
            root.send_message(input.text); 
            input.text = ""; 
        }
    }
}
```
Slint UI 레벨에서 빈 문자열 체크를 추가하여, 내용이 있을 때만 `send_message` 콜백이 호출되도록 함.

---

## 7. 테스트 결과

### 7.1 기능 테스트

| 테스트 항목 | 결과 | 설명 |
|-------------|------|------|
| 서버 기동 | ✅ 성공 | 127.0.0.1:8080 정상 바인딩 |
| GUI 클라이언트 접속 | ✅ 성공 | 서버 연결 및 메시지 송수신 정상 |
| 봇 다중 접속 | ✅ 성공 | 10개 봇 동시 접속 및 인삿말 전송 |
| 메시지 브로드캐스트 | ✅ 성공 | client → 서버 → 모든 봇 수신 확인 |
| 클라이언트 퇴장 처리 | ✅ 성공 | 연결 종료 시 서버 로그 출력 및 태스크 정리 |
| Lagged 에러 복구 | ✅ 성공 | 버퍼 초과 시 서버 크래시 없이 계속 동작 |

### 7.2 실시간 수신 테스트

```
🚀 10명의 봇이 접속을 시작합니다. 모든 수신 메시지를 실시간으로 출력합니다.
📩 [Bot 000 수신]: 127.0.0.1:54321: bot 0 hi
📩 [Bot 001 수신]: 127.0.0.1:54321: bot 0 hi
📩 [Bot 000 수신]: 127.0.0.1:54322: bot 1 hi
...
(client에서 "안녕하세요" 전송 시)
📩 [Bot 000 수신]: 127.0.0.1:54330: 안녕하세요
📩 [Bot 001 수신]: 127.0.0.1:54330: 안녕하세요
...
```

---

## 8. 성능 분석

### 8.1 리소스 사용량

| 지표 | 측정값 (예상) |
|------|-------------|
| 서버 메모리 사용 | ~5-10 MB (10 클라이언트 기준) |
| 봇당 메모리 | ~수 KB (경량 비동기 태스크) |
| CPU 사용률 | 유휴 시 ~0%, 메시지 폭주 시 ~20-30% |

### 8.2 확장성

- 현재 기본 설정: 10개 봇
- `CLIENT_COUNT` 조정으로 최대 수백~수천 봇까지 확장 가능
- OS 파일 디스크립터 제한 해제 시 (`ulimit -n 65535`) 더 많은 동시 접속 지원

---

## 9. 에러 핸들링 전략

| 에러 상황 | 처리 방식 |
|-----------|-----------|
| 클라이언트 연결 종료 | `read_line` 반환값 0 감지 → 태스크 종료 + 로그 출력 |
| Broadcast 버퍼 초과 (Lagged) | `continue`로 건너뛰고 다음 메시지 수신 |
| 소켓 쓰기 실패 | `write_all` 에러 시 태스크 종료 |
| 봇 서버 접속 실패 | `match`로 에러 처리, 해당 봇만 종료 (전체 영향 없음) |
| GUI 닫힌 후 업데이트 시도 | `Weak::upgrade()` 실패 시 안전하게 무시 |

---

## 10. 개선 가능 사항

1. **인증 시스템**: 현재는 IP:Port로만 사용자를 구분하므로, 닉네임/로그인 기능 추가 가능
2. **채팅방 분리**: 단일 broadcast 채널 대신 여러 채널(방)을 운영하여 그룹 채팅 지원
3. **메시지 영속화**: 현재 메시지는 메모리에만 존재하므로, DB 저장으로 채팅 기록 보존
4. **TLS 암호화**: 현재 평문 TCP 통신이므로, `tokio-rustls`를 활용한 암호화 적용
5. **프로토콜 개선**: 줄바꿈(`\n`) 기반 단순 프로토콜 대신 JSON/Protobuf 등 구조화된 프로토콜 도입
6. **재접속 로직**: 클라이언트 연결이 끊겼을 때 자동 재접속 시도 기능

---

## 11. 결론

본 프로젝트는 Rust의 비동기 프로그래밍 모델(`tokio`)과 채널 기반 메시지 전달 패턴(`broadcast`)을 활용하여, **다수의 클라이언트가 동시에 접속하여 실시간으로 메시지를 교환하는 채팅 서버**를 성공적으로 구현하였습니다.

특히 다음 핵심 성과를 달성하였습니다:

1. **확장성**: OS 스레드가 아닌 경량 비동기 태스크를 활용하여 다수의 동시 접속을 효율적으로 처리
2. **안정성**: Lagged 에러, 파일 디스크립터 제한, 스레드 안전성 등 실제 운영 환경에서 발생하는 문제들을 사전에 식별하고 해결
3. **사용성**: Slint GUI 클라이언트를 통해 직관적인 채팅 인터페이스 제공
4. **검증 가능성**: 봇 테스터를 통해 대규모 접속 환경을 시뮬레이션하고 실시간으로 메시지 수신을 확인

---

*작성일: 2026년 5월*
